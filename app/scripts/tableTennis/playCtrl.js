(function (angular, k) {
	'use strict';

	angular.module('ScoreKeeper.TableTennis')
		.controller('play', ['$scope', 'Clash', function ($scope, clash) {
			
		}]);

}).call(this, this.angular, this.H.ScoreKeeper);